---
- name: Restart voice fixture services
  ansible.builtin.systemd_service:
    name: "{{ item }}"
    daemon_reload: true
    enabled: true
    state: restarted
  loop:
    - vivolution-voice-fixture-asterisk.service
    - vivolution-voice-fixture-sipp.service
