---
- name: Install the provider-neutral Vivolution Edge enrollment client locally
  hosts: localhost
  connection: local
  become: true
  gather_facts: true
  roles:
    - role: edge_enrollment_install
