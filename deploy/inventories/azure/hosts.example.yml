# Copy this file to hosts.yml only for the final, explicitly approved Azure
# acceptance test. Azure remains command-only until Jay authorizes that test.
all:
  children:
    controllers:
      hosts:
        cp1:
          ansible_host: controller.voice.vivolution.ae
          ansible_port: 22
          ansible_user: cpadmin
          ansible_ssh_private_key_file: /replace/with/a/dedicated/private/key

